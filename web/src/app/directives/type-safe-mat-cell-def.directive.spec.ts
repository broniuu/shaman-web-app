import { TypeSafeMatCellDef } from './type-safe-mat-cell-def.directive';

describe('TypeSafeMatCellDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeSafeMatCellDef();
    expect(directive).toBeTruthy();
  });
});
