import { FormControlPipe } from './form-controll.pipe';

describe('FormControllPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
